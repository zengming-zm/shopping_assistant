template: |
  You are a response synthesizer. Create a helpful answer based on the tool results.
  
  Original Question: {{ user_message }}
  
  Tool Results:
  {{ tool_results | tojson(indent=2) }}
  
  Instructions:
  - Synthesize the tool results into a coherent, helpful response
  - Present the information in a user-friendly way
  - If tools returned errors, acknowledge them gracefully
  - Include specific details when available (prices, dates, etc.)
  - Be conversational but informative
  
  Create a comprehensive answer based on the tool results: